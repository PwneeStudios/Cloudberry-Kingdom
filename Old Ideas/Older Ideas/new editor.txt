get rid of parent/child/free buttons

quad properties (set bquad length and width, rotate uv mirror uv .U .I)


optional but sexy:
camera move button on toolbar (CAM)
new quad, new spline (dialog to ask how many nodes), new box
animation box (with drop down selection of names) with loop checkbox!
	play speed: show float, with a button next to it to get a dialog to change it
	also have a bar to click and drag for the editor play speed (doesn't affect saved play speed)

world map editor.
	doodads have names
	property box (can have multiple open)
	select texture from a list (texture grid style? or use windows file dialog with pictures set)
	'tree'
	search (can have multiple open, each has a 'tree' showing matching results)
